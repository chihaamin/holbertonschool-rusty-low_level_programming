/*
main - Entry point of the program
This function writes a formatted string to the standard output using the print! macro.
Returns: ()
 */
fn main() -> () {
    // Using the `print!` macro to write a formatted string to standard output
    print!("with proper grammar, but the outcome is a piece of art,\n");
}
